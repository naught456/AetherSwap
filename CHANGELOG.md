# 更新日志 / Changelog

本项目的所有重要变更都会记录在此文件中。本格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 计划中
- 支付宝支付接入
- 移动端 / 响应式 UI 适配
- C5Game、IGXE、悠悠有品等更多交易平台接入
- 多账号并发任务调度

---

## [v0.1.0-beta] - 2026-09-15

### 新增 (Added)

#### 全局仪表盘
- 直观、响应式的 Web 前端仪表盘，所有运行状态、行情看板与系统设置均在图形界面完成，零代码门槛。
- 桌面 / 服务器模式自动判断，本地桌面启动内嵌窗口，无桌面服务器监听 `0.0.0.0:28472`。

#### 自动化倒余额流程
- 端到端 Pipeline：`选品 → 下单 → 入库 → 上架 → Steam Guard 确认`，全程几乎无人工干预。
- 桌面环境通过内嵌浏览器自动完成登录与 Cookie 提取；无桌面 Linux 自动降级为手动 Cookie 流程。
- 绑定 `identity_secret` 后，由程序自动签署商品上架的 Steam Guard 二次确认。

#### 数学模型驱动的选品与定价
- **变异系数 (CV)**：量化价格波动幅度，自动剔除剧烈震荡的高风险品。
- **趋势拟合度 (R²)**：识别持续下跌或走势紊乱的冷门品。
- **参考价计算**：根据 Steam 寄售深度与历史成交数据，动态选取最优上架价格。
- **价格墙定价 / 墙+断层一体定价** 双核心可在策略中心替换。

#### 策略中心
- 独立的策略中心 UI，工作区分为 **购入策略**、**出售策略**、**模块管理**。
- 系统预设策略与用户自定义策略并行：内置策略保证默认流程始终可恢复，自定义策略支持保存、删除、导入、导出。
- 启用策略前显示差异摘要；启用自定义策略前可先"模拟运行"（dry-run）确认模块结果。
- 模块编辑规则：固定模块不可删除、参数在线校验、依赖与互斥校验、长按拖拽排序。
- **声明式用户自定义模块**：仅允许 `declarative` 类型模块启用链，使用白名单操作符读取上下文和前置模块输出，不执行任何用户代码，杜绝高风险动作被任意代码接管。
- 模块数量限制、重复添加、依赖缺失、顺序错误、互斥冲突均被阻止。

#### 实时行情与大盘追踪
- 实时追踪现有饰品折扣状态以及大盘市场变动，便于捕捉最佳交易时机。

#### 历史饰品数据分析
- 深度的历史饰品数据分析，复盘特定饰品在长期时间线上的价格走势、成交量变动及盈亏表现。
- 数据面板以图表形式展示购入成本、上架价格、最终成交额、实际获得的 Steam 余额以及综合折扣比率。

#### Steam 游戏折扣雷达
- 一键批量获取 Steam 平台当前打折游戏数据。
- 按折扣力度、玩家好评率、历史最低价（史低）等维度动态排序与筛选。
- **一键生成 1920×1080 高质量折扣分享卡片**：右键任意游戏卡片即可生成专属分享卡片。

#### 安全与风控
- Steam 移动令牌（Steam Guard）生成与管理模块，支持在控制台直接生成 2FA 验证码。
- 智能代理池中转：支持自定义长效静止 IP 及 Webshare 自动轮换动态 IP，防止同 IP 频繁请求触发社区风控或红信。
- 内嵌 Steam 登录：在面板输入账号即可自动完成登录与 Cookie 提取。
- 多渠道消息推送：内置 PushPlus 微信推送 + 邮件预警，重要事件即时通知。
- 一键出厂重置：彻底清理代理、令牌、数据库、日志等全部隐私数据，安全迁移无忧。

#### 部署与运维
- Docker / Docker Compose 一键部署，无需手动安装 Playwright 系统依赖。
- `config/` 与 `log/` 通过 volume 挂载持久化，容器重建后数据不丢失。
- 容器内以非 root 用户（uid 1000）运行，权限更安全。
- Nginx 反向代理与访问鉴权提示文档，避免管理面板直接暴露公网。

#### 支付确认
- 微信支付全流程接入：扫码支付 → 邮件 IMAP 监听 → 自动流转至"提取核销 + 提醒卖家发货"。
- 未配置邮箱时自动降级为 5 分钟手动确认模式，不会报错或崩溃。
- 可通过 `email_timeout_seconds` 调整默认超时。

### 修复 (Fixed)
- 修复邮件监听超时问题，确保 IMAP 自动确认链路稳定。
- 修复 `typing_extensions` 旧版本导致的 `cannot import name 'Sentinel'` 启动异常（文档提示升级依赖）。

### 文档 (Documentation)
- 全新中文 README：核心功能、快速开始、策略中心教程、工作原理、进阶配置、FAQ、Roadmap、免责声明。
- 提供桌面运行 (`requirements.txt`) 与无桌面服务器 (`requirements-server.txt`) 两套依赖清单。
- 提供 Docker 部署、Linux 服务器部署、环境变量覆盖、单元测试运行说明。
- 新增策略中心使用教程（声明式模块 manifest 字段表 + 常用字段说明）。
- 增补 FAQ：波动率/斜率放弃购买的原因、Buff Cookie 过期处理、声明式模块安全边界等。

### 测试 (Testing)
- 单元测试套件覆盖策略中心与各核心模块，支持 `pytest -q` 一键回归。
- 提供前端脚本语法检查命令（`node --check`）。

---

## 版本说明

- 当前版本：**v0.1.0-beta**
- 项目仍处于 Beta 阶段，API、配置结构与策略模块可能在后续版本调整。
- 在升级前请务必备份 `config/` 目录。

[Unreleased]: https://github.com/naught456/AetherSwap/compare/v0.1.0-beta...HEAD
[v0.1.0-beta]: https://github.com/naught456/AetherSwap/releases/tag/v0.1.0-beta